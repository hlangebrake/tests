import {esc,pct,num,dateTime,duration,initials,progress,stat,signalsHTML,empty} from './view-helpers.js';
export function overview(report){
 const {summary:s,rows,topics}=report;if(!rows.length)return empty('Der erste Lernstand macht den Anfang.','Importiere JSON-Speicherstände oder ein ZIP mit den Dateien deiner Klasse. Es werden keine Beispieldaten ungefragt hinzugefügt.')+`<div class="empty-actions"><button class="primary" data-action="import">Lernstände importieren</button><button class="secondary" data-action="demo">Demoklasse ausprobieren</button></div>`;
 const attention=rows.filter(r=>r.priority>=2).sort((a,b)=>b.priority-a.priority||a.student.studentName.localeCompare(b.student.studentName,'de')).slice(0,4);
 return `<div class="stats-grid">${stat('Lernende',String(s.students),'in der ausgewählten Klasse')}${stat('Quest-Fortschritt',pct(s.averageProgress),'Mittelwert · reguläre Quests','accent')}${stat('Hier genauer hinschauen',String(s.attention),'evidenzbasierte Gesprächsanlässe')}${stat('Versuche pro Bearbeitung',num(s.avgAttempts),'nur beobachtete Aufgabenversuche')}</div>
 <div class="overview-grid"><section class="card"><div class="card-heading"><div><span class="eyebrow">MATHEMATISCHE THEMEN</span><h2>Wo steht die Klasse?</h2></div><button class="text-button" data-nav="topics">Alle Themen →</button></div><div class="topic-bars">${topics.map(t=>`<button class="topic-bar" data-topic="${esc(t.topicId)}"><span><strong>${esc(t.title)}</strong><small>${esc(t.place)} · ${t.completed} / ${t.totalQuests} Quests</small></span>${progress(t.progress)}<span class="topic-meta">Ø ${num(t.avgAttempts)} Versuche</span></button>`).join('')}</div></section>
 <section class="card"><div class="card-heading"><div><span class="eyebrow">NÄCHSTE PÄDAGOGISCHE SCHRITTE</span><h2>Wo sollte ich hinschauen?</h2></div></div><p class="muted small">Hinweise, keine Diagnosen. Die Gründe sind bei jeder Person sichtbar.</p>${attention.length?attention.map(r=>`<button class="attention-person" data-student="${esc(r.student.studentId)}"><span class="avatar">${esc(initials(r.student.studentName))}</span><span><strong>${esc(r.student.studentName)}</strong><small>${esc(r.signals.find(s=>s.priority>=2)?.title)}</small><em>${esc(r.signals.find(s=>s.priority>=2)?.evidence)}</em></span><b>↗</b></button>`).join(''):'<div class="calm-note">Für die vorliegenden Daten werden keine priorisierten Hinweise ausgelöst. Das ersetzt keine eigene Beobachtung.</div>'}<button class="secondary wide" data-nav="attention">Hinweise und Kriterien ansehen</button></section></div>
 <section class="card compact"><div><span class="eyebrow">LERNWEGE SICHTBAR MACHEN</span><h2>Fortschritt ist mehr als richtig oder falsch.</h2><p class="muted">${pct(s.errorRate)} Fehlversuche · ${pct(s.helpRate)} der Bearbeitungen mit selbst gewählter Hilfe · ${s.challengeCompleted} abgeschlossene Meisterherausforderungen.</p></div><button class="secondary" data-nav="timeline">Zeitlichen Verlauf öffnen →</button></section>`;
}
export const SORT_COLUMNS=[['name','Name'],['progress','Fortschritt'],['completed','Quests'],['skips','Für später'],['errors','Fehlerquote'],['attempts','Versuche'],['help','Hilfen'],['duration','Aktive Zeit'],['challenge','Challenges'],['last','Zuletzt aktiv'],['priority','Hinweise']];
export function learnerTable(rows,{query='',signal='all',sort='priority',descending=true}={}){
 const value=(r,key)=>({name:r.student.studentName.toLocaleLowerCase('de'),progress:r.regular.progress,completed:r.regular.completed,skips:r.regular.skipCount,errors:r.regular.errorRate,attempts:r.regular.avgAttempts,help:r.regular.helpRate,duration:r.regular.duration,challenge:r.challenge.completed,last:r.lastActive,priority:r.priority}[key]);
 const filtered=rows.filter(r=>r.student.studentName.toLowerCase().includes(query.toLowerCase())&&(signal==='all'||r.signals.some(s=>s.code===signal)||signal==='attention'&&r.priority>=2)).sort((a,b)=>{const x=value(a,sort),y=value(b,sort);if(x==null)return y==null?0:1;if(y==null)return -1;const v=typeof x==='string'?x.localeCompare(y,'de'):x-y;return (descending?-1:1)*v;});
 return `<div class="table-summary">${filtered.length} von ${rows.length} Lernenden · Zeile öffnen für den vollständigen Lernverlauf</div><div class="table-scroll"><table class="learner-table"><thead><tr>${SORT_COLUMNS.map(([k,n])=>`<th scope="col" aria-sort="${sort===k?(descending?'descending':'ascending'):'none'}"><button data-sort="${k}">${n}${sort===k?(descending?' ↓':' ↑'):''}</button></th>`).join('')}</tr></thead><tbody>${filtered.map(r=>`<tr><th scope="row"><button class="name-cell" data-student="${esc(r.student.studentId)}"><span class="avatar">${esc(initials(r.student.studentName))}</span><span>${esc(r.student.studentName)}<small>${esc(r.student.className||'Ohne Klassenzuordnung')}</small></span></button></th><td>${progress(r.regular.progress)}</td><td>${r.regular.completed??'—'}/${r.regular.totalQuests}</td><td>${r.regular.skipCount}<small>${r.regular.currentlySkipped} noch offen</small></td><td>${pct(r.regular.errorRate)}</td><td>${num(r.regular.avgAttempts)}</td><td>${pct(r.regular.helpRate)}</td><td>${duration(r.regular.duration)}</td><td>${r.challenge.completed??'—'}/${r.challenge.totalQuests}<small>${r.challenge.unlocked} freigeschaltet</small></td><td>${dateTime(r.lastActive)}</td><td><span class="priority p${r.priority}">${r.priority>=3?'Genauer hinsehen':r.priority===2?'Gesprächsanlass':r.signals[0]?.code==='limited'?'Wenig Daten':'Beobachten'}</span></td></tr>`).join('')}</tbody></table></div>`;
}
export function topicsView(topics,skills,selected=''){
 return `<div class="section-intro"><h2>Fachliche Schwerpunkte erkennen</h2><p>Quoten beziehen sich auf reguläre Quests. Herausforderungen und Wegprüfungen werden in den Personenansichten separat ausgewertet.</p></div><div class="topic-grid">${topics.map(t=>`<article class="card topic-card ${selected===t.topicId?'selected-topic':''}"><span class="eyebrow">${esc(t.place)}</span><h2>${esc(t.title)}</h2>${progress(t.progress)}<dl class="metrics-list"><div><dt>Bearbeitet</dt><dd>${pct(t.participation)}</dd></div><div><dt>Aufgaben gelöst</dt><dd>${pct(t.successRate)}</dd></div><div><dt>Ø Versuche</dt><dd>${num(t.avgAttempts)}</dd></div><div><dt>Für später markiert</dt><dd>${pct(t.skipRate)}</dd></div><div><dt>Tipp / Beispiel</dt><dd>${pct(t.hintRate)} / ${pct(t.exampleRate)}</dd></div><div><dt>Ø aktive Zeit</dt><dd>${duration(t.avgDuration)}</dd></div></dl><small class="muted">${t.attempts} beobachtete Antwortversuche</small></article>`).join('')}</div><section class="card"><div class="card-heading"><h2>Einzelne Wissensgedanken</h2><span class="muted">Nach durchschnittlichen Versuchen</span></div><div class="table-scroll"><table><thead><tr><th>Inhalt</th><th>Bearbeitungen</th><th>Ø Versuche</th><th>Fehlerquote</th><th>Mit Hilfe</th></tr></thead><tbody>${skills.filter(s=>!selected||s.topicId===selected).sort((a,b)=>b.avgAttempts-a.avgAttempts).map(s=>`<tr><th>${esc(s.title)}</th><td>${s.runs}</td><td>${num(s.avgAttempts)}</td><td>${pct(s.errorRate)}</td><td>${pct(s.helpRate)}</td></tr>`).join('')}</tbody></table></div></section>`;
}
export function attentionView(rows){
 const sorted=rows.slice().sort((a,b)=>b.priority-a.priority||a.student.studentName.localeCompare(b.student.studentName,'de'));
 return `<div class="section-intro"><h2>Hinsehen, nachfragen, gemeinsam weiterdenken.</h2><p>Es werden beobachtbare Muster beschrieben, keine Lerntypen oder Diagnosen. Automatische Hinweise beruhen ausschließlich auf regulären Aufgaben. Häufige Hilfe allein erzeugt keinen Schwierigkeitshinweis.</p></div><div class="attention-grid">${sorted.map(r=>`<section class="card"><button class="person-heading" data-student="${esc(r.student.studentId)}"><span class="avatar">${esc(initials(r.student.studentName))}</span><span><h2>${esc(r.student.studentName)}</h2><small>${pct(r.regular.progress)} Quest-Fortschritt · ${r.regular.attemptedRuns} beobachtete Bearbeitungen</small></span><b>↗</b></button>${signalsHTML(r.signals)}</section>`).join('')}</div>`;
}
