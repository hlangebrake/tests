import {regulationSummary,sessionReview,GOALS,NEXT_GOALS,STRATEGIES,SKIP_REASONS,SUPPORT_LABELS,SRL_RULES} from '../shared/regulation.js';
import {esc,dateTime,duration,pct,stat} from './view-helpers.js';
export function regulationClassView(rows){const evidence=rows.filter(r=>r.regulation?.hasData),total=(key)=>rows.reduce((n,r)=>n+(r.regulation?.[key]||0),0);return `<section class="calm-note"><strong>Lernentscheidungen sichtbar machen – nicht bewerten.</strong> Ziele, Hilfeentscheidungen und Führung werden getrennt von mathematischen Ergebnissen betrachtet. Es gibt keinen Selbstregulations-Score, keine Diagnose und keinen Nachteil für nicht gewählte Herausforderungen.</section><div class="stats-grid">${stat('Mit Regulationsdaten',`${evidence.length} / ${rows.length}`,'im gewählten Zeitraum')}${stat('Lernpläne',total('plans'),'bewusst gewählt oder abgelehnt')}${stat('Abschlussreflexionen',total('reflections'),'freiwillige Einschätzungen')}${stat('Strategie → später gelöst',total('outcomes'),'zeitliche Folge, kein Kausalnachweis')}</div><section class="card"><h2>Entscheidungen und Unterstützung</h2><p class="muted">Unterstützungsgrade: 3 starke Orientierung → 2 Empfehlung → 1 Auswahlhilfe → 0 selbst gewählte Hilfe. Ein niedriger Grad ist keine Leistungsnote.</p><div class="table-scroll"><table><thead><tr><th>Person</th><th>Navigation</th><th>Eigene Questwahl</th><th>Strategien</th><th>Später gelöst</th><th>Zurückgekehrt</th><th>Reflexionen</th></tr></thead><tbody>${rows.map(r=>{const x=r.regulation||{};return `<tr><th><button class="text-button" data-student="${esc(r.student.studentId)}">${esc(r.student.studentName)}</button></th><td>${x.hasHistory?x.levels.navigation:'—'}</td><td>${x.hasData?x.independent||0:'—'}</td><td>${x.hasData?x.strategies||0:'—'}</td><td>${x.hasData?x.outcomes||0:'—'}</td><td>${x.hasData?x.returned||0:'—'}</td><td>${x.hasData?x.reflections||0:'—'}</td></tr>`;}).join('')}</tbody></table></div>${evidence.length?'':'<p class="muted">Noch keine Regulationsereignisse im Zeitraum. Alte Dateien enthalten diese Daten nicht; fehlende Einträge bedeuten nicht fehlende Selbstregulation.</p>'}</section><section class="card"><h2>Transparente Startregeln</h2><p>Höchstens ${SRL_RULES.maxUnplanned} ungeplante Impulse pro Sitzung und zusätzlich pro ${SRL_RULES.rollingWindowMs/60000}-Minuten-Fenster, mindestens ${SRL_RULES.cooldownMs/60000} Minuten Abstand. Strategieangebot frühestens nach ${SRL_RULES.errorStreak} regulären Fehlversuchen, bei Herausforderungen nach ${SRL_RULES.challengeErrorStreak}. Prüfungen bleiben ohne Lernhilfen.</p><p class="muted">Die konfigurierbaren Schwellenwerte sind Entwurfsentscheidungen, keine empirisch kalibrierten Grenzwerte. Details und Code: <code>shared/regulation.js</code>.</p></section>`;}
export function regulationDetailView(d,range={}){const x=regulationSummary(d.events,range),hasHistory=d.events.some(e=>e.type.startsWith('srl_')&&e.timestamp<=(range.to||'9999')),observed=x.plans.length+x.strategies.length+x.reflections.length+x.navigation.length,topics=new Map(d.student.catalog.topics.map(t=>[t.topicId,t.title]));const sessions=d.sessions.filter(s=>s.startedAt<=(range.to||'9999')&&(s.finishedAt||s.updatedAt)>=(range.from||'')).sort((a,b)=>b.startedAt.localeCompare(a.startedAt));
 const within=d.events.filter(e=>e.timestamp>=(range.from||'')&&e.timestamp<=(range.to||'9999')),bySession=new Map();for(const e of within){if(!bySession.has(e.sessionId))bySession.set(e.sessionId,[]);bySession.get(e.sessionId).push(e);}
 return `<section class="calm-note">Selbsteinschätzungen sind subjektiv; richtige Antworten sind auf die tatsächlich bearbeiteten Aufgaben beschränkt. Hilfe gefolgt von Erfolg beweist keine Ursache. Herausforderungsfehler werden nicht als Defizit der Grundkompetenz gewertet.</section>${!observed?'<section class="card"><p>Noch keine beobachteten Regulationsentscheidungen im gewählten Zeitraum. Die Funktion wurde mit App 5.1 eingeführt.</p></section>':''}<div class="stats-grid">${stat('Pläne',x.plans.length,'einschließlich Planänderungen')}${stat('Strategiewahlen',x.strategies.length,'auf der Aufgabe oder im Impuls')}${stat('Später richtig',x.outcomes.length,'nach einer gewählten Strategie')}${stat('Rückkehr',x.returned,'zu ausdrücklich zurückgestellten Quests')}</div><section class="card"><h2>Planen → Arbeiten → neu planen</h2><p class="muted small">Pro Sitzung nur Ereignisse innerhalb des gewählten Filters. Fehlende Einschätzungen bleiben leer. Eine richtige Antwort nach Lösungsschritten ist keine unabhängige Leistungsprobe.</p><div class="table-scroll"><table><thead><tr><th>Sitzung</th><th>Plan</th><th>Start / Ende</th><th>Reguläre Aufgaben</th><th>Herausforderung</th><th>Nächstes Vorhaben</th></tr></thead><tbody>${sessions.map(s=>{const es=bySession.get(s.sessionId)||[],r=sessionReview(es,s.sessionId,d.student.catalog),plan=r.plan,ref=es.findLast(e=>e.sessionId===s.sessionId&&e.type==='srl_reflection');return `<tr><td>${dateTime(s.startedAt)}</td><td>${esc(GOALS[plan?.goal]||'Nicht erfasst')}<small>${esc(topics.get(plan?.topicId)||'')}${plan?.targetCount?` · ${plan.targetCount===5?'5+':plan.targetCount} Quests`:''}</small></td><td>${plan?.confidence??'—'} / ${ref?.data.confidence??'—'}<small>Skala 1–5</small></td><td>${r.regular.runs} Bearbeitungen<small>${pct(r.regular.firstTry)} erster Versuch richtig</small></td><td>${r.challenge.runs} Bearbeitungen<small>${pct(r.challenge.firstTry)} erster Versuch richtig</small></td><td>${esc(NEXT_GOALS[ref?.data.nextGoal]||'Nicht erfasst')}</td></tr>`;}).join('')}</tbody></table></div></section><section class="card"><h2>Unterstützung im Verlauf</h2><p>${Object.entries(SUPPORT_LABELS).map(([k,label])=>`${esc(label)}: <strong>${hasHistory?x.levels[k]:'—'}</strong>`).join(' · ')}</p><p class="muted small">Stand am Filterende. Fachliche Unterstützung und Regulationshilfe sind getrennte Einstellungen; nicht beobachtete Vorgeschichte ist unbekannt.</p>${x.changes.length?`<ol class="compact-history">${x.changes.map(e=>`<li>${dateTime(e.timestamp)} · ${esc(SUPPORT_LABELS[e.data.dimension])}: ${e.data.previous} → ${e.data.level} · ${esc({student_choice:'selbst eingestellt',accepted_offer:'Angebot angenommen',stable_regular_work_and_choices:'schrittweise reduziert nach regulären Bearbeitungen und eigenen Lernentscheidungen'}[e.data.reason]||e.data.reason)}${e.data.evidence?.runs?` (${e.data.evidence.runs} Aufgabenbearbeitungen, ${e.data.evidence.independent} eigene erfolgreiche Questwahlen)`:''}</li>`).join('')}</ol>`:'<p>Keine Änderung im Filterzeitraum beobachtet.</p>'}</section><div class="overview-grid"><section class="card"><h2>Strategien und spätere Ergebnisse</h2>${x.strategies.length?`<ul class="compact-history">${x.strategies.slice(-100).reverse().map(e=>{const out=x.outcomes.find(o=>o.data.selectionEventId===e.eventId);return `<li>${dateTime(e.timestamp)} · ${esc(STRATEGIES[e.data.strategy]||e.data.strategy)}${out?` → später richtig${out.data.assisted?' (auch Rechenweg genutzt)':''}`:' · danach kein zugeordneter Erfolg im Beobachtungsfenster'}<small>${esc(d.tasks.find(t=>t.taskId===e.taskId)?.title||e.taskId||'')}${e.kind==='challenge'?' · Herausforderung':''}</small></li>`;}).join('')}</ul>`:'<p>Noch keine beobachtete Strategieauswahl.</p>'}<p class="muted small">Es wird nur die zuletzt gewählte Strategie auf derselben Aufgabenbearbeitung vor einem späteren Erfolg zugeordnet, höchstens ${SRL_RULES.strategyOutcomeMs/60000} Minuten später. Weitere Hilfen können mitgewirkt haben.</p></section><section class="card"><h2>Eigene Wege und Vertiefung</h2>${x.skips.map(e=>`<p>${dateTime(e.timestamp)} · ${esc(SKIP_REASONS[e.data.choice]||'Ohne Angabe weiter')}</p>`).join('')}${x.areaDecisions.map(e=>`<p>${dateTime(e.timestamp)} · ${esc(topics.get(e.data.from)||e.data.from)} → ${esc(topics.get(e.data.to)||e.data.to)}: ${e.data.choice==='continue'?'weitergegangen':'noch etwas bearbeitet'}<small>${esc(SKIP_REASONS[e.data.reason]||'Keine Angabe')}</small></p>`).join('')}${x.challenges.map(e=>`<p>${dateTime(e.timestamp)} · Herausforderung: ${esc({accept:'freiwillig gewählt',start:'begonnen',later:'für später aufgehoben',explore:'zunächst weiter erkundet'}[e.data.choice]||e.data.choice)}</p>`).join('')||'<p>Keine neue Herausforderungsauswahl erfasst.</p>'}<p class="muted small">Erkunden, Zurückstellen oder keine Herausforderung zu wählen wird nicht als Mangel bewertet.</p></section></div>`;}
export function compactRegulation(events,range){const r=regulationSummary(events,range);return {hasData:!!(r.plans.length+r.strategies.length+r.reflections.length+r.navigation.length),hasHistory:events.some(e=>e.type.startsWith('srl_')&&e.timestamp<=(range?.to||'9999')),levels:r.levels,plans:r.plans.length,reflections:r.reflections.length,strategies:r.strategies.length,outcomes:r.outcomes.length,returned:r.returned,independent:r.navigation.filter(e=>['map','self','repeat','new','continue','challenge'].includes(e.data.source)).length};}
