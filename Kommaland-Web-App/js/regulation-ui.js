import {CHECKS} from './foundation-checks.js';
import {REVIEW_ACTIONS,GOALS,NEXT_GOALS,STRATEGIES,SKIP_REASONS,SUPPORT_LABELS,calibrationText} from '../teacher-dashboard/shared/regulation.js';
import {esc,icon} from './ui.js';
/** UI adapter, deliberately separate from mathematical grading and the SRL policy. */
export class RegulationUI {
 constructor(reg,adapter){this.reg=reg;this.a=adapter;this.back=null;this.pendingExit=null;}
 release(){this.back=null;}
 handleClose(){if(!this.back)return false;const fn=this.back;this.back=null;fn();return true;}
 open(mode,config,back){this.a.open('srl-'+mode,{tag:'DEIN LERNWEG',symbol:'route',className:'srl-panel',...config});this.back=back;}
 button(label,id,primary=false){return `<button id="${id}" class="${primary?'primary':'secondary'}-button">${esc(label)}</button>`;}
 choices(items,selected,attr='srl-choice'){return `<div class="srl-choices">${items.map(([value,label,sub])=>`<button type="button" data-${attr}="${esc(value)}" class="srl-choice ${selected===value?'selected':''}" aria-pressed="${selected===value}"><strong>${esc(label)}</strong>${sub?`<small>${esc(sub)}</small>`:''}</button>`).join('')}</div>`;}
 confidence(value,id){return `<div class="srl-confidence" role="group" aria-label="Sicherheit von 1 bis 5">${[1,2,3,4,5].map(n=>`<button type="button" data-confidence="${n}" class="${n===value?'selected':''}" aria-pressed="${n===value}" aria-label="${n} von 5: ${['noch sehr unsicher','eher unsicher','teils sicher','ziemlich sicher','sehr sicher'][n-1]}">${n}</button>`).join('')}<input type="hidden" id="${id}" value="${value||''}"></div><div class="srl-scale-labels"><span>noch unsicher</span><span>sehr sicher</span></div>`;}
 bindConfidence(id,onChange=()=>{}){document.querySelectorAll('[data-confidence]').forEach(b=>b.onclick=()=>{document.getElementById(id).value=b.dataset.confidence;document.querySelectorAll('[data-confidence]').forEach(x=>{x.classList.toggle('selected',x===b);x.setAttribute('aria-pressed',x===b);});onChange(+b.dataset.confidence);});}
 planning({amend=false}={}){
  this.reg.sync();const a=this.a,old=this.reg.plan(),prior=this.reg.intention(),draft={goal:old?.goal||'self',topicId:old?.topicId||a.region(),targetCount:old?.targetCount||null,confidence:old?.confidence||null};
  const render=()=>{this.open('plan',{title:amend?'Dein Plan darf sich ändern.':'Was möchtest du heute machen?',body:`${!amend&&prior?`<div class="srl-reminder"><span>LETZTES MAL WOLLTEST DU …</span><strong>${esc(NEXT_GOALS[prior.nextGoal]||'weiterlernen')}</strong><button id="srlResumeIntention" class="text-button">Damit starten</button><small>Oder wähle heute etwas anderes.</small></div>`:''}<p class="srl-lead">Ein kleiner Plan genügt. Du darfst später anders entscheiden.</p>${this.reg.levels.planning===0?'<details class="srl-optional"><summary>Heute ein Ziel wählen <small>freiwillig</small></summary>':''}${this.choices(Object.entries(GOALS),draft.goal)}<label class="srl-field" id="srlTopicWrap" ${draft.goal==='self'?'hidden':''}>Dein Thema <select id="srlTopic">${a.regions.map(r=>`<option value="${r.id}" ${r.id===draft.topicId?'selected':''}>${esc(r.topic)} · ${esc(r.name)}</option>`).join('')}</select></label>${this.reg.levels.planning===0?'</details>':''}<details class="srl-optional" ${amend&&draft.targetCount?'open':''}><summary>Ungefähr wie viele Quests? <small>freiwillig</small></summary><div class="srl-amounts">${[2,3,4,5].map(n=>`<button data-amount="${n}" class="${draft.targetCount===n?'selected':''}" aria-pressed="${draft.targetCount===n}">${n===5?'5+':n}</button>`).join('')}<button data-amount="" class="${draft.targetCount===null?'selected':''}">Nicht festlegen</button></div></details><h3>Wie sicher fühlst du dich beim gewählten Thema?</h3>${this.confidence(draft.confidence,'srlStartConfidence')}<p class="small-note">Keine Note. Du kannst die Einschätzung auch offenlassen.</p>`,foot:this.button(amend?'Zurück ohne Änderung':'Heute ohne Plan starten','srlPlanSkip')+this.button(amend?'Plan übernehmen':"Los geht’s",'srlPlanStart',true)},()=>skip());
   document.querySelectorAll('[data-srl-choice]').forEach(b=>b.onclick=()=>{draft.goal=b.dataset.srlChoice;render();});document.getElementById('srlTopic').onchange=e=>draft.topicId=e.target.value;
   document.querySelectorAll('[data-amount]').forEach(b=>b.onclick=()=>{draft.targetCount=b.dataset.amount?+b.dataset.amount:null;document.querySelectorAll('[data-amount]').forEach(x=>{x.classList.toggle('selected',x===b);x.setAttribute('aria-pressed',x===b);});});
   this.bindConfidence('srlStartConfidence',n=>draft.confidence=n);
   document.getElementById('srlPlanStart').onclick=()=>save();document.getElementById('srlPlanSkip').onclick=skip;
   const resume=document.getElementById('srlResumeIntention');if(resume)resume.onclick=()=>{draft.goal={continue:'topic',challenge:'challenge',open:'open',explore:'explore'}[prior.nextGoal]||'self';draft.topicId=prior.topicId||a.region();draft.targetCount=null;save(true);};
  };
  const save=(resume=false)=>{this.reg.setPlan({...draft,topicId:draft.goal==='self'?null:draft.topicId,source:resume?'previous_intention':'self',confidence:draft.confidence});this.release();a.closeWorld();a.refresh();if(resume||draft.goal!=='self')this.navigateForGoal(draft.goal,draft.topicId,resume?'previous_intention':'plan');};
  const skip=()=>{if(!amend)this.reg.setPlan({goal:'self',topicId:null,targetCount:null,confidence:null,source:'declined'});this.release();a.closeWorld();a.refresh();};render();
 }
 navigateForGoal(goal,topic,source){const a=this.a;if(goal==='explore'){a.showMap();return;}if(goal==='self')return;const candidates=a.quests.filter(q=>(!topic||q.region===topic)&&(goal==='challenge'?q.challenge&&a.unlocked(q):!q.challenge&&!a.state().completed[q.id]&&a.unlocked(q)));const q=candidates[0];if(q){this.reg.choose(q,source);a.walk(q.id);}else{a.toast(goal==='challenge'?'Die Herausforderung ist hier noch nicht offen. Du kannst zuerst reguläre Quests wählen.':'In diesem Bereich ist alles bearbeitet. Du kannst wiederholen oder weiterreisen.');a.showMap(topic);}}
 afterQuest(q,replay=false){this.reg.fade();this.a.refresh();const a=this.a,s=a.state(),newChallenge=!q.challenge&&!replay&&a.quests.filter(x=>x.region===q.region&&!x.challenge).every(x=>s.completed[x.id]),challenge=a.quests.find(x=>x.region===q.region&&x.challenge),foot=document.querySelector('.panel-foot'),body=document.querySelector('.success-body');
  if(newChallenge&&challenge&&!s.completed[challenge.id]){const card=document.createElement('section');card.className='srl-challenge-offer';card.innerHTML=`<span class="section-tag">VERTIEFEN · FREIWILLIG</span><h3>Herausforderung freigeschaltet</h3><p>Alle regulären Quests dieses Gebiets sind erledigt. Jetzt kannst du dein Wissen an schwierigeren Problemen erproben.</p>${this.reg.plan()?.confidence>=4&&this.reg.levels.challenge>0?'<p>Du hast dich heute sicher eingeschätzt. Hier kannst du diese Einschätzung an neuen Problemen ausprobieren – ohne etwas beweisen zu müssen.</p>':''}<p class="small-note">Kein Hindernis auf deinem weiteren Weg. Schwierigkeiten gehören hier dazu.</p><div class="srl-inline-buttons">${this.button('Herausforderung starten','srlChallengeStart',true)}${this.button('Später','srlChallengeLater')}${this.button('Weiter erkunden','srlChallengeExplore')}</div>`;body?.append(card);
   document.getElementById('srlChallengeStart').onclick=()=>{this.reg.challenge(challenge,'accept');this.reg.choose(challenge,'challenge');a.walk(challenge.id);};for(const [id,choice] of [['srlChallengeLater','later'],['srlChallengeExplore','explore']])document.getElementById(id).onclick=()=>{this.reg.challenge(challenge,choice);a.closeWorld();};return;}
  if(replay||q.challenge||!foot)return;
  const level=this.reg.levels.navigation;if(level===0)return;const next=a.recommended(q.region);if(!next)return;
  const box=document.createElement('section');box.className='srl-next';box.innerHTML=level===3?`<span>ALS NÄCHSTES</span><strong>${esc(next.title)}</strong><p>Diese Quest baut auf passenden Gedanken auf. Du kannst auch selbst wählen.</p>${this.button('Zum Infoschild gehen','srlNextGo',true)}${this.button('Selbst auf der Karte wählen','srlNextOwn')}`:level===2?`<h3>Wie möchtest du weitermachen?</h3><p>Empfohlen: <strong>${esc(next.title)}</strong></p>${this.button('Zur empfohlenen Quest','srlNextGo',true)}${this.button('Andere Möglichkeiten','srlNextOptions')}${this.button('Selbst wählen','srlNextOwn')}`:`<h3>Was möchtest du als Nächstes machen?</h3>${this.button('Möglichkeiten ansehen','srlNextOptions',true)}${this.button('Selbst auf der Karte wählen','srlNextOwn')}`;body?.append(box);
  const go=document.getElementById('srlNextGo');if(go)go.onclick=()=>{this.reg.choose(next,'recommended');if(level===3)a.walkInfo(next);else a.walk(next.id);};document.getElementById('srlNextOwn').onclick=()=>a.closeWorld();const opt=document.getElementById('srlNextOptions');if(opt)opt.onclick=()=>this.navigation();
 }
 navigation(reason='requested',promptId=null){const a=this.a,reg=this.reg,region=a.region(),next=a.recommended(region),repeat=a.quests.find(q=>q.region===region&&a.state().completed[q.id]&&!q.challenge)||a.quests.find(q=>a.state().completed[q.id]&&!q.challenge),fresh=a.quests.find(q=>q.region!==region&&!q.challenge&&!a.state().completed[q.id]&&a.unlocked(q)),challenge=a.quests.find(q=>q.challenge&&a.unlocked(q)&&!a.state().completed[q.id]);
  const choose=(q,source)=>{if(promptId)reg.answerPrompt(promptId,reason,source);this.release();if(!q){a.showMap();return;}reg.choose(q,source);a.walk(q.id);};
  this.open('navigation',{title:reason==='requested'?'Hilf mir bei der Auswahl.':'Möchtest du Hilfe bei der Auswahl?',body:`<p>${reason==='new_topic'?'Du bist in einem neuen Gebiet. Eine passende Einstiegsquest ist verfügbar.':'Du entscheidest. Hier sind einige Möglichkeiten für deinen nächsten Schritt.'}</p>${this.choices([['continue','Weiterüben',next?.title||'Auf der Karte auswählen'],['repeat','Etwas wiederholen',repeat?.title||'Einen Gedanken am Infoschild nachlesen'],['new','Etwas Neues ausprobieren',fresh?.title||'Einen neuen Bereich erkunden'],...(challenge?[['challenge','Eine Herausforderung versuchen',challenge.title]]:[]),['self','Selbst auf der Karte wählen']])}${reason!=='requested'?'<div class="srl-offer-structure"><p>Mehr Orientierung auf der Karte?</p><button id="srlMoreStructure" class="secondary-button">Passende Quests wieder empfehlen</button></div>':''}<p class="small-note">Wegprüfungen bleiben bestehen. Diese Auswahl verändert keine Aufgaben oder Freischaltungen.</p>`,foot:this.button('Unterstützung einstellen','srlSupport')+this.button('Ich suche selbst weiter','srlNavClose',true)},()=>choose(null,'self'));
  document.querySelectorAll('[data-srl-choice]').forEach(b=>b.onclick=()=>{const source=b.dataset.srlChoice;if(source==='self'){if(promptId)reg.answerPrompt(promptId,reason,'self');this.release();a.closeWorld();return;}choose({continue:next,repeat,new:fresh,challenge}[source],source);});
  document.getElementById('srlNavClose').onclick=()=>{if(promptId)reg.answerPrompt(promptId,reason,'self');this.release();a.closeWorld();};document.getElementById('srlSupport').onclick=()=>{if(promptId)reg.answerPrompt(promptId,reason,'settings');this.settings();};const more=document.getElementById('srlMoreStructure');if(more)more.onclick=()=>{reg.changeSupport('navigation',Math.max(2,reg.levels.navigation),'accepted_offer');choose(next,'recommended');};
 }
 maybe(reason){const candidate=this.reg.candidate(reason);if(!candidate)return false;const id=this.reg.prompt(reason,candidate.context);if(!id)return false;if(reason==='errors')this.strategyReview(id,reason,candidate.context,()=>this.strategy(id,reason,candidate.context));else if(reason==='skips')this.skipReason(id);else this.navigation(reason,id);return true;}
 strategyReview(promptId,reason,context,next,back=null){
  const selected=new Set(),help=this.reg.helpEvidence(context),observed=[];
  if(help.hint)observed.push(`${help.hint} Tipp-Aufruf${help.hint===1?'':'e'}`);if(help.example)observed.push(`${help.example} Beispiel-Aufruf${help.example===1?'':'e'}`);if(help.knowledge)observed.push(`${help.knowledge} bewusste Wissensöffnung${help.knowledge===1?'':'en'}`);
  const done=()=>{this.reg.reviewStrategy(promptId,[...selected],context);next();};
  this.open('review',{title:'Was hast du schon versucht?',body:`<p>Es gab mehrere Fehlversuche. Ein kurzer Blick auf deinen bisherigen Weg – dann geht es mit Mathematik weiter.</p>${observed.length?`<p class="evidence-note">Bei dieser Aufgabenbearbeitung geöffnet: ${esc(observed.join(' · '))}. Was könntest du zusätzlich verändern?</p>`:''}<div class="review-options">${Object.entries(REVIEW_ACTIONS).map(([id,label])=>`<button class="secondary-button" data-review-action="${id}" aria-pressed="false">${esc(label)}</button>`).join('')}</div><p class="small-note">Mehrere Angaben sind möglich. Die Auswahl ist freiwillig; sie ist keine Bewertung.</p>`,foot:this.button('Jetzt eine Strategie wählen','srlReviewNext',true)},back||done);
  document.querySelectorAll('[data-review-action]').forEach(b=>b.onclick=()=>{const key=b.dataset.reviewAction;if(selected.has(key))selected.delete(key);else{if(key==='guess')selected.clear();else selected.delete('guess');selected.add(key);}document.querySelectorAll('[data-review-action]').forEach(x=>{const yes=selected.has(x.dataset.reviewAction);x.classList.toggle('selected',yes);x.setAttribute('aria-pressed',String(yes));});});document.getElementById('srlReviewNext').onclick=done;
 }
 examReview(result,choose,back,context){
  const candidate=this.reg.candidate('exam_errors',context);if(!candidate)return false;const ctx=candidate.context,id=this.reg.prompt('exam_errors',ctx);if(!id)return false;
  const options=()=>{const done=key=>{this.reg.answerPrompt(id,'exam_errors',key,ctx);const skill=document.getElementById('srlExamSkill')?.value||result.failed[0];this.release();choose(key,skill);};
   this.open('exam-strategy',{title:'Was wäre jetzt ein sinnvoller nächster Schritt?',body:`<p>Die offenen Kernkompetenzen stehen in deinem Prüfungsbericht. Wähle gezielt eine davon. Richtige Nachweise bleiben erhalten.</p><label class="srl-field">Gezielt nacharbeiten<select id="srlExamSkill">${result.failed.map(id=>`<option value="${id}">${esc(CHECKS[id].title)}</option>`).join('')}</select></label>${this.choices([['quest','Die passende Quest bearbeiten'],['knowledge','Das zugehörige Wissen nachlesen'],['example','Ein Beispiel ansehen'],['person','Hilfe vor Ort holen'],['later','Später weiterlernen']])}<p class="small-note">Die Prüfrunde ist beendet. Die Hilfe öffnet außerhalb der Prüfung.</p>`,foot:this.button('Zurück zum Prüfungsbericht','srlExamBack',true)},back);document.querySelectorAll('[data-srl-choice]').forEach(b=>b.onclick=()=>done(b.dataset.srlChoice));document.getElementById('srlExamBack').onclick=back;
  };this.strategyReview(id,'exam_errors',ctx,options,back);return true;
 }
 strategy(promptId=null,reason='requested',context=null){const a=this.a,s=a.session();if(!s||s.exam||s.solved)return;context??=this.reg.context();if(!promptId)promptId=this.reg.prompt(reason,context,true);const challenge=s.q.challenge,help=this.reg.helpEvidence(context);let options=[['reread','Ich lese die Aufgabe noch einmal'],['knowledge','Ich sehe mir das passende Wissen an'],['hint','Ich möchte einen kleinen Hinweis'],['example','Ich schaue mir ein ähnliches Beispiel an'],['check','Ich prüfe meinen bisherigen Rechenweg'],['steps','Ich gehe die Aufgabe schrittweise durch'],['drawing','Ich nutze eine Skizze oder Darstellung'],['person','Ich hole mir Hilfe'],['later','Ich versuche es später noch einmal']];if(help.hint||help.example){options.sort((a,b)=>(help[a[0]]?1:0)-(help[b[0]]?1:0));}const choose=strategy=>{this.reg.answerPrompt(promptId,reason,strategy,context);if(strategy==='continue'){this.release();a.showTask();return;}this.reg.selectStrategy(strategy,{source:reason,context});this.release();a.strategyAction(strategy);};
  this.open('strategy',{title:challenge?'Das ist eine knifflige Herausforderung.':'Was könnte dir jetzt helfen?',body:`<p>${challenge?'Schwierigkeiten sind bei diesen Transferaufgaben zu erwarten. Sie sagen nicht, dass du die Grundlagen nicht kannst.':'Wähle einen kleinen nächsten Schritt. Du musst nicht sofort die ganze Lösung kennen.'}</p>${help.example?'<p class="evidence-note">Du hast bereits ein Beispiel gesehen. Vergleiche jetzt deinen eigenen Rechenweg damit oder probiere eine andere Darstellung.</p>':help.hint?'<p class="evidence-note">Du hast bereits einen Tipp gelesen. Vielleicht hilft jetzt eine andere Strategie statt desselben Tipps.</p>':''}${this.choices(options)}<p class="small-note">Die Unterstützung bleibt freiwillig. Dein bisheriger Aufgabenfortschritt bleibt erhalten.</p>`,foot:this.button('Ich probiere selbst weiter','srlKeepTrying',true)},()=>choose('continue'));
  document.querySelectorAll('[data-srl-choice]').forEach(b=>b.onclick=()=>choose(b.dataset.srlChoice));document.getElementById('srlKeepTrying').onclick=()=>choose('continue');
 }
 skipReason(id){const reg=this.reg,done=choice=>{reg.answerPrompt(id,'skips',choice);this.release();this.a.closeWorld();};this.open('skip',{title:'Du hast einige Quests für später aufgehoben.',body:`<p>Was passt gerade am besten? Deine Entscheidung ist in Ordnung.</p>${this.choices(Object.entries(SKIP_REASONS).filter(([k])=>k!=='unspecified'))}`,foot:this.button('Ohne Angabe weiter','srlSkipContinue',true)},()=>done('unspecified'));document.querySelectorAll('[data-srl-choice]').forEach(b=>b.onclick=()=>done(b.dataset.srlChoice));document.getElementById('srlSkipContinue').onclick=()=>done('unspecified');}
 exit(from,to,onContinue){const a=this.a,open=a.quests.filter(q=>q.region===from&&!q.challenge&&!a.state().completed[q.id]);if(!open.length||!this.reg.mayAskExit(from,to))return false;const id=this.reg.prompt('area_exit',{topicId:from});if(!id)return false;const done=choice=>{const reason=document.getElementById('srlExitReason')?.value||'unspecified';this.reg.answerPrompt(id,'area_exit',choice,{topicId:from});this.reg.areaDecision(from,to,choice,reason);this.release();a.closeWorld();if(choice==='continue')onContinue();else{const q=open.find(q=>a.unlocked(q))||open[0];this.reg.choose(q,'continue');a.walk(q.id);}};
  this.open('exit',{title:`In ${a.regions.find(r=>r.id===from)?.name} sind noch ${open.length} Quests offen.`,body:`<p>Du darfst weitergehen und später zurückkommen. Offene Quests gehen nicht verloren.</p><label class="srl-field">Warum möchtest du weitergehen? <small>freiwillig</small><select id="srlExitReason"><option value="unspecified">Keine Angabe</option>${['known','return','change','difficult'].map(k=>`<option value="${k}">${esc(SKIP_REASONS[k])}</option>`).join('')}</select></label>`,foot:this.button('Noch etwas bearbeiten','srlStay')+this.button('Weitergehen','srlLeave',true)},()=>{this.reg.answerPrompt(id,'area_exit','dismissed',{topicId:from});this.release();a.closeWorld();});document.getElementById('srlStay').onclick=()=>done('stay');document.getElementById('srlLeave').onclick=()=>done('continue');return true;
 }
 settings(){const a=this.a,reg=this.reg,labels={navigation:['Freie Karte · Hilfe auf Abruf','Mehrere Möglichkeiten','Eine Empfehlung','Starke Orientierung'],planning:['Kompakter Start','Plan anbieten','Plan und Themenwahl','Plan und Themenwahl'],strategy:['Nur wenn ich frage','Seltene Impulse','Bei wiederholten Schwierigkeiten','Bei wiederholten Schwierigkeiten'],content:['Hilfen nur auf Abruf','Kleinen Hinweis hervorheben','Beispiel als nächsten Hilfeschritt hervorheben','Schrittweises Vorgehen hervorheben'],challenge:['Sachliche Einladung','Optionale Einschätzung verbinden','Optionale Einschätzung verbinden','Optionale Einschätzung verbinden']};
  this.open('settings',{title:'So viel Unterstützung wie du brauchst.',body:`<p>Du kannst jederzeit mehr oder weniger Unterstützung wählen. Neue mathematische Gedanken bleiben erhalten.</p>${Object.entries(SUPPORT_LABELS).map(([k,title])=>`<label class="srl-field">${esc(title)}<select data-support="${k}">${labels[k].map((v,n)=>`<option value="${n}" ${reg.levels[k]===n?'selected':''}>${n} · ${esc(v)}</option>`).join('')}</select></label>`).join('')}<p class="small-note">Nur die Navigationsführung wird automatisch schrittweise reduziert – wenn sichere reguläre Bearbeitungen und eigene erfolgreiche Lernentscheidungen zusammenkommen. Mehr Führung wird angeboten, nicht erzwungen. Fachliche Hilfen bleiben selbst gewählt.</p>`,foot:this.button('Übernehmen','srlSettingsDone',true)},()=>{this.release();a.closeWorld();});document.getElementById('srlSettingsDone').onclick=()=>{document.querySelectorAll('[data-support]').forEach(el=>reg.changeSupport(el.dataset.support,+el.value));this.release();a.closeWorld();a.refresh();};
 }
 finish(){const a=this.a,reg=this.reg,review=reg.review(),p=review.plan;let confidence=null;const nextTopic=p?.topicId||a.region();const secure=review.topics.filter(t=>t.runs>=3&&t.firstTry>=.8).map(t=>t.title),hard=review.topics.filter(t=>t.runs>=3&&t.firstTry<.6).map(t=>t.title),strategies=[...new Set(review.strategies.map(x=>x.strategy))];
  this.open('reflection',{title:'Dein Lernweg heute.',tag:'ABSCHLIESSEN · ZURÜCKBLICKEN · WEITERPLANEN',body:`<div class="srl-review-grid"><section><span>DEIN PLAN</span><strong>${esc(p?GOALS[p.goal]:'Kein Plan festgelegt')}</strong><p>${p?.topicId?esc(a.regions.find(r=>r.id===p.topicId)?.topic||''):''}${p?.targetCount?` · ungefähr ${p.targetCount===5?'5+':p.targetCount} Quests`:''}</p></section><section><span>DAS HAST DU GEMACHT</span><strong>${review.worked} ${review.worked===1?'Quest':'Quests'} bearbeitet</strong><p>${review.completed} reguläre abgeschlossen · ${review.skipped} für später markiert</p></section></div><div class="srl-review-notes">${secure.length?`<p><strong>Oft gleich richtig:</strong> ${esc(secure.join(', '))} – bezogen auf die heute bearbeiteten Aufgaben.</p>`:''}${hard.length?`<p><strong>Hier brauchtest du mehrere Anläufe:</strong> ${esc(hard.join(', '))}.</p>`:''}<p><strong>Vertiefung:</strong> ${review.unlocked} Herausforderungen neu freigeschaltet, ${review.challengeStarted} ausprobiert, ${review.challengesCompleted} abgeschlossen.</p>${review.challenge.errors?'<p>Die Transferaufgaben waren teilweise anspruchsvoll. Das ist getrennt von den regulären Grundlagen zu betrachten.</p>':''}<p><strong>Genutzte Hilfen:</strong> ${review.hints} Tipp-Aufrufe · ${review.examples} Beispiel-Aufrufe.</p>${strategies.length?`<p><strong>Danach konntest du weiterarbeiten:</strong> ${esc(strategies.map(k=>STRATEGIES[k]).join(', '))}. ${review.strategies.some(x=>x.assisted)?'Teilweise wurde auch der Rechenweg gezeigt.':''}</p><small>Wir sehen die Reihenfolge, nicht die Ursache des Erfolgs.</small>`:''}</div><section class="srl-reflection-question"><h3>1. Wie sicher fühlst du dich jetzt?</h3><p>${p?.topicId?esc(a.regions.find(r=>r.id===p.topicId)?.topic||'Dein gewähltes Thema'):'Bei deinen heute bearbeiteten Themen'}${p?.confidence?` · am Start: ${p.confidence} von 5`:''}</p>${this.confidence(null,'srlEndConfidence')}<p id="srlCalibration" class="srl-calibration" aria-live="polite">Eine Einschätzung ist freiwillig. Es gibt keine Note.</p></section><section class="srl-reflection-question"><h3>2. Was möchtest du beim nächsten Mal machen?</h3><label class="srl-field"><span class="sr-only">Nächstes Vorhaben</span><select id="srlNextGoal"><option value="later">Beim nächsten Mal entscheiden</option>${Object.entries(NEXT_GOALS).filter(([k])=>k!=='later').map(([k,v])=>`<option value="${k}">${esc(v)}</option>`).join('')}</select></label></section><p class="small-note">Dein Plan ist keine Verpflichtung. Die Angaben werden mit dem Lernstand für deine Lehrkraft exportiert.</p>`,foot:this.button('Noch weiterlernen','srlFinishBack')+this.button('Abschließen und exportieren','srlFinishExport',true)},()=>{this.release();a.closeWorld();});this.bindConfidence('srlEndConfidence',n=>{confidence=n;document.getElementById('srlCalibration').textContent=calibrationText(review,n);});document.getElementById('srlFinishBack').onclick=()=>{this.release();a.closeWorld();};document.getElementById('srlFinishExport').onclick=()=>{reg.reflect({confidence,nextGoal:document.getElementById('srlNextGoal').value,topicId:nextTopic,startConfidence:p?.confidence||null});this.release();a.exportFinished();};
 }
}
