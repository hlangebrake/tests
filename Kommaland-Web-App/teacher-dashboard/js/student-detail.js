import {studentSummary,metrics} from './analytics.js';
import {filterEvents,EVENT_LABELS} from './timeline.js';
import {esc,pct,num,dateTime,duration,progress,stat,status,signalsHTML,initials} from './view-helpers.js';
export function detailView(d,range={},tab='overview',taskPage=0,taskFilter='worked',eventPage=0,eventCategory='all'){
 const r=studentSummary(d.student,d,range),m=r.regular,c=r.challenge;
 const tabs=[['overview','Überblick'],['quests','Quests & Aufgaben'],['timeline','Zeitleiste'],['sessions','Sitzungen']];
 let body='';
 if(tab==='overview')body=`<div class="stats-grid">${stat('Reguläre Quests',`${m.completed??'—'} / ${m.totalQuests}`,pct(m.progress)+' abgeschlossen','accent')}${stat('Ø Versuche',num(m.avgAttempts),m.attemptedRuns+' beobachtete Bearbeitungen')}${stat('Fehlversuche',pct(m.errorRate),m.errors+' von '+m.attempts+' Versuchen')}${stat('Selbst gewählte Hilfe',pct(m.helpRate),'Tipps: '+m.hintOpenCount+' · Beispiele: '+m.exampleOpenCount)}</div><div class="overview-grid"><section class="card"><h2>Gesprächsanlässe</h2>${signalsHTML(r.signals)}<p class="muted small">Geringer Fortschritt allein und Fehler in Herausforderungen lösen keine Schwierigkeitshinweise aus.</p></section><section class="card"><h2>Herausforderungen & Prüfungen</h2><dl class="metrics-list"><div><dt>Challenges freigeschaltet</dt><dd>${c.unlocked} / ${c.totalQuests}</dd></div><div><dt>Challenges begonnen</dt><dd>${c.challengeStarted}</dd></div><div><dt>Challenges abgeschlossen</dt><dd>${c.completed??'—'}</dd></div><div><dt>Challenge-Versuche</dt><dd>${c.attempts}</dd></div><div><dt>Challenge-Hilfen</dt><dd>${pct(c.helpRate)}</dd></div><div><dt>Wegprüfungen bestanden</dt><dd>${r.exam.completed??'—'}</dd></div><div><dt>Prüfungs-Fehlversuche</dt><dd>${r.exam.errors} / ${r.exam.attempts}</dd></div></dl><p class="calm-note">Anspruchsvolle Zusatzaufgaben werden nicht als Test fehlender Grundkompetenz interpretiert.</p></section></div><section class="card"><h2>Nach Themenbereich</h2><div class="table-scroll"><table><thead><tr><th>Thema</th><th>Quests</th><th>Gelöste Bearbeitungen</th><th>Ø Versuche</th><th>Hilfen</th><th>Aktive Zeit</th></tr></thead><tbody>${d.student.catalog.topics.map(t=>{const x=metrics(d,{...range,topicId:t.topicId});return `<tr><th>${esc(t.title)}</th><td>${progress(x.progress)}</td><td>${pct(x.successRate)}</td><td>${num(x.avgAttempts)}</td><td>${pct(x.helpRate)}</td><td>${duration(x.duration)}</td></tr>`;}).join('')}</tbody></table></div></section>`;
 if(tab==='quests'){
  const filtered=d.tasks.filter(t=>taskFilter==='all'||taskFilter==='worked'&&t.startedAt||taskFilter==='errors'&&t.attempts.some(a=>!a.correct)||taskFilter==='help'&&t.helps.some(h=>!h.automatic));
  const pageTasks=filtered.slice(taskPage*50,(taskPage+1)*50);
  body=`<section class="card"><h2>Questverlauf</h2><p class="muted small">„Für später“ ist eine ausdrückliche Auswahl. „Unterbrochen“ bedeutet nur, dass eine Aufgabe verlassen wurde. Frühere Erfolge bleiben in der Historie erhalten.</p><div class="quest-detail-grid">${d.quests.map(q=>`<details class="quest-detail"><summary><span>${q.kind==='challenge'?'✦ ':q.kind==='exam'?'◇ ':''}${esc(q.title)}</span>${status(q.status)}</summary><dl class="metrics-list"><div><dt>Erstmals begonnen</dt><dd>${dateTime(q.firstStartedAt)}</dd></div><div><dt>Zuletzt bearbeitet</dt><dd>${dateTime(q.lastWorkedAt)}</dd></div><div><dt>Abgeschlossen</dt><dd>${dateTime(q.completedAt)}</dd></div><div><dt>Durchläufe / für später</dt><dd>${q.runs.length} / ${q.skipCount}</dd></div></dl>${q.baseline?'<p class="muted small">Vorhandener Stand übernommen. Frühere Versuche und Zeiten sind unbekannt.</p>':''}<ul class="compact-history">${q.history.map(e=>`<li>${dateTime(e.timestamp)} · ${esc(EVENT_LABELS[e.type]||e.type)}</li>`).join('')}</ul></details>`).join('')}</div></section><section class="card"><div class="card-heading"><h2>Einzelne Aufgaben</h2><label>Anzeige <select id="taskFilter"><option value="worked" ${taskFilter==='worked'?'selected':''}>Bearbeitet</option><option value="all" ${taskFilter==='all'?'selected':''}>Alle Aufgaben</option><option value="errors" ${taskFilter==='errors'?'selected':''}>Mit Fehlversuchen</option><option value="help" ${taskFilter==='help'?'selected':''}>Mit Hilfen</option></select></label></div><p class="muted small">Zeiten: aktive Vordergrundzeit, keine Geschwindigkeitseinstufung. Antwortinhalte werden nicht gespeichert.</p>${pageTasks.map(t=>`<details class="task-detail"><summary><span><strong>${esc(t.title)}</strong><small>${esc(t.skillTitle||t.skillId)} · ${esc(t.kind)} · ${esc(t.type)}</small></span><span>${t.attemptCount} Versuche · ${duration(t.duration)}<small>${t.hintOpenCount} Tipps · ${t.exampleOpenCount} Beispiele</small></span>${status(t.status)}</summary><p class="technical-id">Aufgabe ${esc(t.taskId)}</p>${t.runs.map(run=>`<div class="run-block"><h3>${dateTime(run.startedAt)} · ${run.attempts.length} Versuche · ${duration(run.duration)}</h3><ol class="attempts">${run.attempts.map(a=>`<li>${dateTime(a.timestamp)} · Versuch ${a.attemptNumber}: <strong>${a.correct?'richtig':'noch nicht richtig'}</strong></li>`).join('')}</ol>${run.helps.map(h=>`<p class="help-entry">${esc({hint:'Tipp',example:'Beispiel',solutionStep:'Lösungsschritt',otherHelp:'Wissen bewusst geöffnet',knowledge:'Automatisch eingeführtes Wissen'}[h.helpType]||h.helpType)} · ${dateTime(h.openedAt)} · ${duration(h.duration)} aktive Zeit${h.closedAt?' · geschlossen '+dateTime(h.closedAt):' · beim Export noch offen'}${h.endEstimated?' · Ende geschätzt':''}${h.resumed?' · wieder sichtbar, keine neue Anforderung':''}</p>`).join('')}</div>`).join('')||'<p>Noch keine beobachtete Bearbeitung.</p>'}</details>`).join('')}<div class="pagination"><button class="secondary" data-taskpage="${taskPage-1}" ${taskPage===0?'disabled':''}>← Zurück</button><span>${filtered.length?taskPage*50+1:0}–${Math.min((taskPage+1)*50,filtered.length)} von ${filtered.length}</span><button class="secondary" data-taskpage="${taskPage+1}" ${(taskPage+1)*50>=filtered.length?'disabled':''}>Weiter →</button></div></section>`;
 }
 if(tab==='timeline'){
  const events=filterEvents(d.events,{...range,category:eventCategory}),pageEvents=events.slice(eventPage*100,(eventPage+1)*100),questMap=new Map(d.quests.map(q=>[q.questId,q.title]));
  body=`<section class="card"><div class="card-heading"><div><h2>Zeitlicher Lernverlauf</h2><p class="muted">${events.length} Ereignisse im Zeitraum · neueste zuerst</p></div><label>Ereignisse <select id="eventCategory">${[['all','Alle'],['quests','Quests & Prüfungen'],['attempts','Antwortversuche'],['help','Hilfeöffnungen'],['sessions','Sitzungen']].map(([id,title])=>`<option value="${id}" ${eventCategory===id?'selected':''}>${title}</option>`).join('')}</select></label></div><ol class="timeline-list">${pageEvents.map(e=>`<li><time>${dateTime(e.timestamp)}</time><span class="timeline-dot ${e.type.includes('completed')?'success':''}"></span><div><strong>${esc(EVENT_LABELS[e.type]||e.type)}</strong><p>${esc(questMap.get(e.questId)||d.student.catalog.topics.find(t=>t.topicId===e.topicId)?.place||'')}${e.type==='task_attempt'?` · Versuch ${e.data.attemptNumber}: ${e.data.correct?'richtig':'noch nicht richtig'}`:''}</p>${e.taskId?`<small>${esc(d.tasks.find(t=>t.taskId===e.taskId)?.title||e.taskId)}</small>`:''}${e.type==='history_baseline'?'<small>Ab hier gibt es beobachtete Verlaufsdaten; ältere Versuche sind unbekannt.</small>':''}</div></li>`).join('')}</ol><div class="pagination"><button class="secondary" data-eventpage="${eventPage-1}" ${eventPage===0?'disabled':''}>← Zurück</button><span>${events.length?eventPage*100+1:0}–${Math.min(events.length,(eventPage+1)*100)} von ${events.length}</span><button class="secondary" data-eventpage="${eventPage+1}" ${(eventPage+1)*100>=events.length?'disabled':''}>Weiter →</button></div></section>`;
 }
 if(tab==='sessions')body=`<section class="card"><h2>Lernsitzungen</h2><p class="muted">Die Dauer zählt aktive Vordergrundzeit. Nach 90 Sekunden ohne Interaktion pausiert die Messung. Browser-Hintergrundzeit zählt nicht. Angezeigt wird jeweils die gesamte Sitzung, die den Filterzeitraum berührt.</p><div class="table-scroll"><table><thead><tr><th>Beginn</th><th>Ende</th><th>Aktive Dauer</th><th>Abschluss</th></tr></thead><tbody>${d.sessions.filter(s=>s.startedAt<=range.to&&(!range.from||(s.finishedAt||s.updatedAt)>=range.from)).sort((a,b)=>b.startedAt.localeCompare(a.startedAt)).map(s=>`<tr><td>${dateTime(s.startedAt)}</td><td>${dateTime(s.finishedAt)}</td><td>${duration(s.duration)}</td><td>${esc({unit_finished:'Lerneinheit abgeschlossen',page_hidden_or_closed:'Seite verlassen',interrupted:'Unterbrochen; Ende nur geschätzt',import:'Spielstand geladen',state_replaced:'Spielstand gewechselt',profile_changed:'Profil gewechselt'}[s.finishReason]||'Beim Export noch offen')}</td></tr>`).join('')}</tbody></table></div></section>`;
 return `<div class="detail-heading"><button class="text-button" data-nav="students">← Zur Klasse</button><div class="person-heading"><span class="avatar large">${esc(initials(d.student.studentName))}</span><div><span class="eyebrow">INDIVIDUELLER LERNVERLAUF</span><h2>${esc(d.student.studentName)}</h2><p>Zuletzt aktiv: ${dateTime(r.lastActive)} · Daten bis ${dateTime(d.student.latestExportAt)}</p></div></div><details class="identity-details"><summary>Identität & lokale Verwaltung</summary><p class="technical-id">Interne ID: ${esc(d.student.studentId)}</p><label>Klasse / Gruppe <input id="studentClass" maxlength="60" value="${esc(d.student.className||'')}" placeholder="z. B. 6a"></label><button class="secondary" id="saveClass">Zuordnung speichern</button><button class="danger" id="deleteStudent">Daten dieser Person löschen</button></details></div><nav class="detail-tabs">${tabs.map(([id,label])=>`<button data-detailtab="${id}" class="${tab===id?'active':''}">${label}</button>`).join('')}</nav>${!d.knownState?'<div class="calm-note">Vor diesem Zeitpunkt liegt noch kein beobachteter Lernstand vor. Offene Inhalte sind hier nicht als nachgewiesenes Nichtbearbeiten zu verstehen.</div>':''}${body}`;
}
