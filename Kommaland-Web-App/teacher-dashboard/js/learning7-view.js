import {compactDifficulty,difficultyClassView,difficultyDetailView} from './difficulty-view.js';
import {PRACTICE_CATALOG} from '../shared/practice-catalog.js';
import {learning7Projection,EVIDENCE_LABELS,WORK_STATUSES,CONFIDENCE_LABELS,PRACTICE_RULES} from '../shared/learning7.js';
import {LEARNING_GOALS,PRACTICE_SKILLS,OPEN_WORK,DIAGNOSTIC_IMPULSES} from '../shared/learning7-content.js';
import {esc,dateTime,duration,empty} from './view-helpers.js';
const practiceTitle=key=>PRACTICE_CATALOG.find(t=>t.key===key)?.title||key;
const goal=id=>Object.values(LEARNING_GOALS).find(g=>g.goalId===id);
/** Describes evidence; never infers the student's conception from clicks alone. */
export function diagnosticCard(e,{always=false}={}){
 const d=DIAGNOSTIC_IMPULSES[e.skillId],title=PRACTICE_SKILLS[e.skillId]?.title;if(!d)return '';
 return `<article class="v7-diagnostic"><h3>${esc(title)}</h3>${e.conversation?`<p class="v7-observation"><strong>Diagnoseanlass:</strong> ${e.errorCount} Fehlversuche bei ${e.errorTasks} unterschiedlichen Aufgaben, insgesamt ${e.attempts} Versuche im Zeitraum.</p>`:always?'<p class="muted small">Gesprächsimpuls auf Wunsch – kein automatisch festgestellter Fehler.</p>':''}<p class="small">Lernspur bis zum Stichtag: <strong>${esc(EVIDENCE_LABELS[e.status])}</strong></p><details><summary>Fachdiagnostische Hinweise anzeigen</summary><h4>Mögliche Fehlvorstellung – im Gespräch prüfen</h4><p>${esc(d.possible)}</p><h4>Zwei Diagnosefragen</h4><ol>${d.questions.map(q=>`<li>${esc(q)}</li>`).join('')}</ol><h4>Worauf achten?</h4><p>${esc(d.watch)}</p><p class="muted small">Eine Fehlantwort kann auch durch Lesen, Bedienung oder einen Flüchtigkeitsfehler entstehen. Die Beobachtung ist kein Nachweis dieser Fehlvorstellung.</p></details></article>`;
}
export function compactLearning7(events,range={}){
 const v=learning7Projection(events,range);return {difficulty:compactDifficulty(events,range),rounds:v.rounds.length,finishedRounds:v.rounds.filter(r=>r.finishedAt).length,practiceAttempts:v.rounds.reduce((n,r)=>n+r.tasks.reduce((a,t)=>a+t.attempts.length,0),0),works:v.works.map(w=>({workId:w.workId,status:w.status})),reflections:v.goals.length,evidence:v.evidence};
}
export function learning7ClassView(rows){
 if(!rows.length)return empty('Noch keine fachlichen Lernspuren.','Importiere einen Lernstand. Lernziele, Zusatzübungen und offene Aufträge werden seit Version 7 getrennt erfasst.');
 const persons=rows.filter(r=>r.learning7?.evidence.some(e=>e.conversation));
 return difficultyClassView(rows)+`<section class="card v7-evidence-intro"><span class="eyebrow">FACHLICHER BLICK · KOMMALAND 7.2</span><h2>Von der Beobachtung zum Gespräch.</h2><p>Fehlversuche geben einen Anlass zum Nachfragen, keine Diagnose. Zusatzübung, Selbsteinschätzungen und offene Arbeit verändern die Quote der regulären Quests nicht.</p><p class="muted small">Auslöser: mindestens ${PRACTICE_RULES.diagnosticErrors} Fehlversuche zu einem Gedanken im gewählten Zeitraum. Challenges und freiwillige Zusatzübungen werden nicht als fehlende Grundkompetenz gewertet. Die Schwelle ist ein anpassbarer Startwert.</p></section><section class="card"><h2>Wo lohnt sich ein fachliches Gespräch?</h2>${persons.length?persons.map(r=>`<div class="v7-person-row"><button class="student-link" data-student="${esc(r.student.studentId)}">${esc(r.student.studentName)}</button><span>${r.learning7.evidence.filter(e=>e.conversation).map(e=>esc(PRACTICE_SKILLS[e.skillId].title)).join(' · ')}</span></div>`).join(''):'<p class="muted">Im gewählten Zeitraum kein automatischer Anlass nach diesen Regeln. Wenige oder fehlende Daten sind kein Kompetenznachweis.</p>'}</section><section class="card"><h2>Fachdiagnostische Gesprächskarten</h2><div class="v7-diagnostic-grid">${Object.keys(PRACTICE_SKILLS).map(k=>{const es=rows.map(r=>r.learning7?.evidence.find(e=>e.skillId===k)).filter(Boolean),affected=es.filter(e=>e.conversation).length;if(!affected)return '';const merged={skillId:k,status:'unobserved',conversation:true,errorCount:es.reduce((n,e)=>n+e.errorCount,0),errorTasks:es.reduce((n,e)=>n+e.errorTasks,0),attempts:es.reduce((n,e)=>n+e.attempts,0)};return `<div><p class="muted small">${affected} von ${rows.length} Lernenden mit wiederholten Fehlversuchen. Zählwerte unten: gesamte Klasse.</p>${diagnosticCard(merged)}</div>`;}).join('')||'<p class="muted">Keine Häufung oberhalb der eingestellten Schwelle.</p>'}</div><details class="v7-library"><summary>Alle 17 Gesprächskarten auf Wunsch öffnen</summary><div class="v7-diagnostic-grid">${Object.keys(PRACTICE_SKILLS).map(k=>diagnosticCard({skillId:k,status:'unobserved',conversation:false},{always:true})).join('')}</div></details></section><section class="card"><h2>Ergänzende Lernaktivitäten – separat vom Kern</h2><div class="table-scroll"><table><thead><tr><th>Name</th><th>Zusatzrunden</th><th>Davon beendet</th><th>Offene Aufträge begonnen</th><th>Lernziel-Angaben</th></tr></thead><tbody>${rows.map(r=>`<tr><th><button class="student-link" data-student="${esc(r.student.studentId)}">${esc(r.student.studentName)}</button></th><td>${r.learning7?.rounds||0}</td><td>${r.learning7?.finishedRounds||0}</td><td>${r.learning7?.works.length||0}</td><td>${r.learning7?.reflections||0}</td></tr>`).join('')}</tbody></table></div><p class="muted small">„Begonnen“ bedeutet nicht fachlich richtig. Ohne Version-7-Ereignisse ist die frühere Bearbeitung unbekannt, nicht negativ zu bewerten.</p></section>`;
}
export function learning7DetailView(d,range={}){
 const v=learning7Projection(d.events,range),observed=v.evidence.filter(e=>e.conversation);
 return difficultyDetailView(d,range)+`<section class="card"><h2>Fachdiagnostische Gesprächsanlässe</h2><p>Aktuelle Fehlerzählungen beziehen sich auf den Filterzeitraum. Die Beschreibung bestätigter Anwendungen berücksichtigt den Verlauf bis zum Enddatum.</p><div class="v7-diagnostic-grid">${observed.map(e=>diagnosticCard(e)).join('')||'<p class="muted">Keine automatische Häufung nach den eingestellten Regeln. Das ersetzt kein Gespräch über mathematische Vorstellungen.</p>'}</div><details class="v7-library"><summary>Weitere fachliche Gesprächskarten und Lernspuren</summary>${v.evidence.filter(e=>!e.conversation).map(e=>diagnosticCard(e,{always:true})).join('')}</details><p class="muted small">Eine Wegprobe liefert nur „in einer Stichprobe richtig“. „Mehrfach gezeigt“ erfordert mindestens zwei unterschiedliche reguläre Aufgaben. „Erneut nach Abstand“ erfordert zusätzlich mindestens sieben Tage zwischen beobachteten richtigen Anwendungen. Diese Labels sind weder Note noch vollständige Kompetenzdiagnose; Hilfen und den konkreten Lösungsweg zusätzlich betrachten.</p></section><section class="card"><h2>Ziele und Selbsteinschätzungen</h2>${v.goals.length?`<ol class="v7-goal-history">${v.goals.slice().reverse().map(g=>`<li><time>${dateTime(g.at)}</time><strong>${esc(goal(g.goalId)?.text||g.goalId)}</strong><span>${g.type==='v7_goal_selected'?'Als Ziel gewählt':'Nach der Quest eingeschätzt'} · ${g.value||g.confidence?esc(CONFIDENCE_LABELS[(g.value||g.confidence)-1]):'ohne Sicherheitseinschätzung'}</span></li>`).join('')}</ol>`:'<p class="muted">Noch keine dokumentierte Zieleinschätzung in diesem Zeitraum.</p>'}<p class="muted small">Selbstauskunft, keine bestätigte Leistung.</p></section><section class="card"><h2>Freiwillige Zusatzübungen</h2><p>Die Runden sind endlich und unabhängig von Kernquests und Challenges. Auch eine beendete Runde kann ausgelassene Aufgaben enthalten.</p>${v.rounds.map(r=>`<details class="task-detail"><summary><span><strong>${esc(PRACTICE_SKILLS[r.skillId].title)}</strong><small>${dateTime(r.startedAt)} · ${r.finishedAt?'Runde beendet':'noch offen'}</small></span><span>${r.tasks.filter(t=>t.correct).length}/${r.tasks.length} gelöst · ${duration(r.duration)}</span></summary><p>${r.tasks.filter(t=>t.skipped).length} Aufgaben ausgelassen · ${r.helps.filter(h=>!h.resumed).length} bewusste Hilfeöffnungen · Rückblick: ${esc({safer:'Ich bin jetzt sicherer',more:'Noch etwas üben',return:'Zurück zum Lernweg'}[r.reflection]||'keine Angabe')}</p><div class="table-scroll"><table><thead><tr><th>Aufgabe</th><th>Versuche</th><th>Fehler</th><th>Hilfen</th><th>Aktive Zeit</th><th>Stand</th></tr></thead><tbody>${r.tasks.map(t=>`<tr><th>${esc(practiceTitle(t.taskKey))}</th><td>${t.attempts.length}</td><td>${t.attempts.filter(a=>!a.correct).length}</td><td>${t.helpCount}</td><td>${duration(t.duration)}</td><td>${t.correct?'gelöst':t.skipped?'ausgelassen':'offen'}</td></tr>`).join('')}</tbody></table></div><ol class="compact-history">${r.tasks.flatMap(t=>t.attempts.map(a=>({key:t.taskKey,...a}))).sort((a,b)=>a.timestamp.localeCompare(b.timestamp)).map(a=>`<li>${dateTime(a.timestamp)} · ${esc(practiceTitle(a.key))} · Versuch ${a.attemptNumber}: ${a.correct?'richtig':'noch nicht richtig'}</li>`).join('')}</ol></details>`).join('')||'<p class="muted">Keine beobachtete Zusatzrunde im Zeitraum.</p>'}${v.decisions.length?`<details><summary>Entscheidungen zu Zusatzübungen</summary><ol class="compact-history">${v.decisions.map(x=>`<li>${dateTime(x.at)} · ${esc(PRACTICE_SKILLS[x.skillId].title)} · ${esc({now:'jetzt üben',later:'später üben',declined:'ohne Zusatzübung weiter'}[x.choice])}</li>`).join('')}</ol></details>`:''}</section><section class="card"><h2>Eigene Erklärungen und Partneraufträge</h2><p>Keine automatische Bewertung. Status und Kriterien sind Angaben der lernenden Person. Texte können im fachlichen Gespräch betrachtet werden.</p>${v.works.map(w=>{const meta=OPEN_WORK.find(x=>x.id===w.workId);return `<details class="task-detail v7-work-detail"><summary><span><strong>${esc(meta.title)}</strong><small>${dateTime(w.lastAt)} · ${esc(WORK_STATUSES[w.status])}</small></span></summary><p>${esc(meta.prompt)}</p><h4>Eigene Notiz</h4><blockquote class="v7-note">${esc(w.note||'Keine digitale Notiz. Eine Bearbeitung auf Papier oder mündlich ist möglich.')}</blockquote><h4>Selbstkontrolle</h4><ul>${meta.criteria.map((c,i)=>`<li>${w.checks[i]?'Als gezeigt markiert':'Nicht markiert'}: ${esc(c)}</li>`).join('')}</ul><p>Muster zum Vergleichen: ${w.modelSeen?'geöffnet':'keine Öffnung aufgezeichnet'}</p><ol class="compact-history">${w.history.map(x=>`<li>${dateTime(x.at)} · ${x.status?esc(WORK_STATUSES[x.status]):x.type==='v7_work_model'?'Muster geöffnet':'Begonnen'}</li>`).join('')}</ol></details>`;}).join('')||'<p class="muted">Noch keine offene Bearbeitung im gewählten Zeitraum dokumentiert.</p>'}</section>`;
}
