import test from 'node:test';
import assert from 'node:assert/strict';
import {installMemoryIDB} from './analytics/memory-idb.js';
import {validateSave,parseLearningSave,FORMAT,filename,uid,LIMITS} from '../teacher-dashboard/shared/save-schema.js';
import {CATALOG} from '../teacher-dashboard/shared/catalog.js';
import {project} from '../teacher-dashboard/shared/projection.js';
import {metrics,signals,RULES} from '../teacher-dashboard/js/analytics.js';
import {classHistory,dateRange} from '../teacher-dashboard/js/timeline.js';
import {TeacherDB,planMerge} from '../teacher-dashboard/js/database.js';
import {importFiles} from '../teacher-dashboard/js/importer.js';
import {inspectZip,jsonFilesInZip} from '../teacher-dashboard/js/zip-importer.js';
import {makeDemoSave} from '../teacher-dashboard/js/demo.js';
import {parseSave,defaultState,validateState} from '../js/state.js';
import {QUESTS} from '../js/content.js';
import {report} from '../teacher-dashboard/js/report.js';
installMemoryIDB();
const fresh=()=>new TeacherDB('test:'+uid());
const sample=()=>makeDemoSave(1);
const file=(data,name='save.json')=>new File([JSON.stringify(data)],name,{type:'application/json'});
const projection=s=>project({studentId:s.studentId,catalog:s.catalog,events:s.events,snapshots:[s]});
test('Katalog enthält exakt die bestehenden Quests und Aufgaben ohne Lösungen',()=>{assert.equal(CATALOG.quests.length,69);assert.equal(CATALOG.tasks.length,494);assert.equal(new Set(CATALOG.tasks.map(x=>x.taskId)).size,494);assert(!JSON.stringify(CATALOG).includes('"answer"'));assert.deepEqual(CATALOG.quests.filter(q=>q.active).map(q=>q.questId),QUESTS.map(q=>q.id));});
test('Exportname enthält sicheren Anzeigenamen und lokales Datum',()=>{assert.equal(filename('Max Mustermann',new Date(2026,8,10,13)),'Kommaland_Speicherstand_Max_Mustermann_2026-09-10.json');assert(!filename('../../ A:B').includes('/'));});
test('Alte Spielstände bleiben unverändert ladbar',()=>{const old=defaultState();old.schemaVersion=2;assert.equal(validateState(old).schemaVersion,3);assert.equal(parseSave(JSON.stringify(defaultState())).app,'kommaland');});
test('Namens- und stabile ID-Daten bleiben in lokalem Spielstand erhalten',()=>{const s=defaultState();s.learner={studentId:uid(),studentName:'Max M.'};assert.deepEqual(validateState(s).learner,s.learner);});
test('Neue Lernstandsdatei ist auch wieder in Schüler-App importierbar',()=>{const s=sample();const parsed=parseSave(JSON.stringify(s));assert.equal(parsed.learner.studentId,s.studentId);assert.equal(Object.keys(parsed.completed).length,Object.keys(s.currentState.completed).length);});
test('Pflichtfelder, Versionen und fremde IDs werden streng validiert',()=>{const s=sample();for(const change of [{schemaVersion:999},{studentName:''},{studentId:''},{events:null},{sessions:null},{format:'other'}])assert.throws(()=>validateSave({...s,...change}));const wrong=structuredClone(s);wrong.events[0].studentId='other';assert.throws(()=>validateSave(wrong));});
test('Fehlende Sitzungsreferenz und doppelte Event-IDs sind ungültig',()=>{const s=sample();s.events[0].sessionId='missing';assert.throws(()=>validateSave(s));const s2=sample();s2.events.push({...s2.events[0]});assert.throws(()=>validateSave(s2));});
test('Prototype-Schlüssel in Ereignisdaten werden abgelehnt',()=>{const s=sample();s.events[0].data=JSON.parse('{"__proto__":{"polluted":true}}');assert.throws(()=>validateSave(s));assert.equal({}.polluted,undefined);});
test('Keine Metriken aus Datei übernommen: manipulierte Aggregate werden ignoriert',()=>{const s=sample();s.taskProgress=[{correct:true,attemptCount:0}];s.questProgress=[{status:'completed'}];const v=validateSave(s);assert.equal(v.taskProgress,undefined);assert.ok(metrics(projection(v)).errors>0);});
test('Ein importierter Lernstand enthält aktuelle und historische Projektionen',async()=>{const db=fresh(),s=sample();const r=await db.merge(s,'eins.json');assert.equal(r.status,'imported');assert.equal((await db.all('events')).length,s.events.length);assert.equal((await db.all('questProgress')).length,69);assert.equal((await db.all('taskProgress')).length,494);});
test('Fünffacher Import ist für Lernereignisse, Sessions und Projektionen idempotent',async()=>{const db=fresh(),s=sample();for(let i=0;i<5;i++){const r=await db.merge(s,'gleich.json');assert.equal(r.status,i?'duplicate':'imported');}assert.equal((await db.all('events')).length,s.events.length);assert.equal((await db.all('sessions')).length,s.sessions.length);assert.equal((await db.all('students')).length,1);assert.equal((await db.all('imports')).length,5);});
test('Späterer Export ergänzt nur neue Events und aktualisiert offene Sitzung',async()=>{const db=fresh(),s=sample(),later=structuredClone(s);const last=later.sessions.at(-1);s.sessions.at(-1).finishedAt=null;s.sessions.at(-1).duration-=100;s.sessions.at(-1).updatedAt=new Date(Date.parse(last.updatedAt)-100).toISOString();await db.merge(s,'alt.json');later.exportId=uid();later.exportTimestamp=new Date(Date.parse(later.exportTimestamp)+20000).toISOString();later.events.push({eventId:uid(),studentId:later.studentId,sessionId:last.sessionId,timestamp:new Date(Date.parse(last.updatedAt)+1000).toISOString(),type:'profile_updated',data:{}});last.updatedAt=later.events.at(-1).timestamp;const r=await db.merge(later,'neu.json');assert.equal(r.eventsAdded,1);assert.equal((await db.all('events')).length,later.events.length);assert.ok((await db.all('sessions')).at(-1).finishedAt);});
test('Ältere Importe setzen neueren Namen und Spielstand nicht zurück',async()=>{const db=fresh(),old=sample(),next=structuredClone(old);next.studentName='Neuer Name';next.currentState.learner.studentName=next.studentName;next.exportId=uid();next.exportTimestamp=new Date(Date.parse(old.exportTimestamp)+10000).toISOString();await db.merge(next,'neu.json');await db.merge(old,'alt.json');assert.equal((await db.student(old.studentId)).studentName,'Neuer Name');});
test('Gleicher Name mit zwei Schüler-IDs bleibt getrennt',async()=>{const db=fresh(),a=sample(),b=makeDemoSave(2);b.studentName=a.studentName;b.currentState.learner.studentName=a.studentName;await db.merge(a,'a');await db.merge(b,'b');assert.equal((await db.all('students')).length,2);});
test('Kollision einer eventId bricht gesamten Import atomar ab',async()=>{const db=fresh(),s=sample();await db.merge(s,'ok');const bad=structuredClone(s);bad.exportId=uid();bad.events[0].data={changed:true};await assert.rejects(db.merge(bad,'bad'));assert.equal((await db.all('events')).length,s.events.length);assert.equal((await db.all('snapshots')).length,1);});
test('Konkurrierende Importe derselben Datei erzeugen keine doppelten Daten',async()=>{const db=fresh(),s=sample();await Promise.all([db.merge(s,'a'),db.merge(s,'b')]);assert.equal((await db.all('events')).length,s.events.length);});
test('JSON-Mehrfachimport isoliert ungültige Dateien',async()=>{const db=fresh(),s=sample();const r=await importFiles([file(s),file(s,'dup.json'),new File(['{invalid'],'kaputt.json'),new File(['x'],'notes.txt')],db);assert.equal(r.imported,1);assert.equal(r.duplicate,1);assert.equal(r.invalid,1);assert.equal(r.ignored,1);});
test('ZIP-Import durchsucht tiefe Ordner, ignoriert andere Dateien und dedupliziert',async()=>{const db=fresh(),s=sample(),zip=new globalThis.JSZip();zip.file('klasse/tag01/a/b/c/schueler.json',JSON.stringify(s));zip.file('dup.json',JSON.stringify(s));zip.file('bilder/a.png','irrelevant');zip.file('kaputt.json','{broken');const data=await zip.generateAsync({type:'uint8array',compression:'DEFLATE'});const result=await importFiles([new File([data],'klasse.zip')],db);assert.equal(result.imported,1);assert.equal(result.duplicate,1);assert.equal(result.invalid,1);assert.equal((await db.all('events')).length,s.events.length);});
test('ZIP-Größengrenze verhindert übergroße dekomprimierte JSON-Einträge',async()=>{const z=new globalThis.JSZip();z.file('a.json','{}');const bytes=await z.generateAsync({type:'uint8array',compression:'STORE'}),v=new DataView(bytes.buffer);for(let p=0;p<bytes.length-46;p++)if(v.getUint32(p,true)===0x02014b50){v.setUint32(p+24,LIMITS.jsonBytes+1,true);break;}assert.throws(()=>inspectZip(bytes),/Sicherheitsgrenze/);});
test('ZIP-Prüfsumme erkennt geänderte Dateidaten',async()=>{const z=new globalThis.JSZip();z.file('a.json','{"test":1}');const bytes=await z.generateAsync({type:'uint8array',compression:'STORE'});const entries=inspectZip(bytes),p=entries[0].offset;const v=new DataView(bytes.buffer),dataStart=p+30+v.getUint16(p+26,true)+v.getUint16(p+28,true);bytes[dataStart]^=1;const got=[];for await(const e of jsonFilesInZip(new File([bytes],'bad.zip')))got.push(e);assert.match(got[0].error,/Prüfsumme/);});
test('Leere und beschädigte ZIP-Dateien stürzen nicht ab',async()=>{const db=fresh(),z=new globalThis.JSZip();z.file('readme.txt','x');let r=await importFiles([new File([await z.generateAsync({type:'uint8array'})],'leer.zip'),new File(['not-a-zip'],'kaputt.zip')],db);assert.equal(r.ignored,1);assert.equal(r.invalid,1);});
test('Aufgabenversuche, Hilfezählung und aktive Dauer werden aus Events berechnet',()=>{const s=makeDemoSave(0),p=projection(s),t=p.tasks.find(t=>t.attemptCount);assert.ok(t.attemptCount>=3);assert.equal(t.hintOpenCount,1);assert.equal(t.totalHintDuration,14000);assert.ok(t.duration>=t.totalHintDuration);assert.equal(t.attempts[0].correct,false);assert.equal(t.correct,true);});
test('Automatische Wissenserklärungen zählen nicht als selbstgewählte Hilfe',()=>{const s=sample(),p=projection(s);for(const t of p.tasks)for(const r of t.runs)for(const h of r.helps){h.automatic=true;h.helpType='knowledge';}assert.equal(metrics(p).helpRate,0);});
test('Challenges lösen keine negativen Grundkompetenz-Hinweise aus',()=>{const s=makeDemoSave(4),p=projection(s),regular=metrics(p);assert.equal(regular.errorRate,0);assert.ok(signals(regular,metrics(p,{kind:'challenge'})).some(s=>s.code==='stretch'));assert.ok(!signals(regular,{...metrics(p,{kind:'challenge'}),errorRate:1}).some(s=>s.code==='support'));});
test('Häufige Hilfen allein erzeugen keine Schwierigkeitshinweise',()=>{const p=projection(makeDemoSave(4)),m={...metrics(p),helpRate:1,successRate:1,errorRate:0};assert.ok(!signals(m,metrics(p,{kind:'challenge'})).some(s=>['support','help'].includes(s.code)));});
test('Wenige Daten werden nicht zu einer Diagnose hochgerechnet',()=>{const m={...metrics(projection(sample())),attemptedRuns:2,errorRate:1,avgAttempts:5,helpRate:1,successRate:0};const s=signals(m,{completed:0});assert.ok(s.some(s=>s.code==='limited'));assert.ok(!s.some(s=>['support','help'].includes(s.code)));});
test('Historischer Stand vor der ersten Beobachtung ist unbekannt',()=>{const s=sample(),p=project({studentId:s.studentId,catalog:s.catalog,events:s.events,snapshots:[s],asOf:'2026-08-01T00:00:00.000Z'});assert.equal(p.knownState,false);assert.equal(metrics(p).progress,null);});
test('Zeitfilter verändert Fehlerdaten, aber rekonstruiert Fortschritt bis zum Ende',()=>{const s=sample(),p=projection(s),a=metrics(p),b=metrics(p,{from:'2026-09-02T00:00:00.000Z'});assert.ok(b.attempts<a.attempts);assert.ok(b.duration<a.duration);assert.equal(b.progress,a.progress);});
test('Zeitleiste rekonstruiert echte frühere Questabschlüsse aus Events',()=>{const s=sample(),d={student:{studentId:s.studentId,catalog:s.catalog},...projection(s),snapshots:[s]};const h=classHistory([d]);assert.ok(h.length>=2);assert.ok(h[0].progress<h.at(-1).progress);});
test('Komplette Lehrkräfte-Sicherung ist wieder importierbar und idempotent',async()=>{const a=fresh(),s=sample();await a.merge(s,'s');await a.setClass(s.studentId,'6a');const backup=await a.backup(),b=fresh();const r=await importFiles([file(backup,'backup.json')],b);assert.equal(r.invalid,0);assert.equal((await b.all('events')).length,s.events.length);assert.equal((await b.student(s.studentId)).className,'6a');await importFiles([file(backup,'backup.json')],b);assert.equal((await b.all('events')).length,s.events.length);});
test('Personenbezogenes Löschen entfernt Ereignisse, Projektionen und Importprotokolle',async()=>{const db=fresh(),s=sample(),s2=makeDemoSave(2);await db.merge(s,'s');await db.merge(s2,'s2');await db.deleteStudent(s.studentId);assert.equal((await db.all('students')).length,1);for(const name of ['events','sessions','questProgress','taskProgress','snapshots','imports'])assert.equal((await db.forStudent(name,s.studentId)).length,0);});
test('Klassenbericht aggregiert echte Daten und enthält Themen plus Wissensgedanken',async()=>{const db=fresh();await db.merge(sample(),'s');await db.merge(makeDemoSave(2),'s2');const r=await report(db,{});assert.equal(r.summary.students,2);assert.equal(r.topics.length,8);assert.ok(r.skills.length>1);assert.ok(r.summary.avgAttempts>1);});
test('ZIP mit ungültigem UTF-8 wird als Fehler isoliert, weitere Dateien werden importiert',async()=>{const z=new globalThis.JSZip(),s=sample();z.file('kaputt.json',new Uint8Array([0xff,0xfe,0x80]));z.file('gueltig.json',JSON.stringify(s));const r=await importFiles([new File([await z.generateAsync({type:'uint8array'})],'utf8.zip')],fresh());assert.equal(r.invalid,1);assert.equal(r.imported,1);});
test('Sitzungsende nach Absturz kann auch bei gleichem updatedAt ergänzt werden',async()=>{const s=sample(),later=structuredClone(s);s.sessions.at(-1).finishedAt=null;s.sessions.at(-1).finishReason=null;const db=fresh();await db.merge(s,'vorher.json');later.exportId=uid();later.exportTimestamp=new Date(Date.parse(later.exportTimestamp)+1000).toISOString();await db.merge(later,'danach.json');assert.equal((await db.forStudent('sessions',s.studentId)).find(x=>x.sessionId===later.sessions.at(-1).sessionId).finishedAt,later.sessions.at(-1).finishedAt);});
test('Nicht lesbare Einzeldateien verhindern nachfolgenden Import nicht',async()=>{const broken={name:'lesefehler.json',size:20,text:()=>Promise.reject(new Error('read failed'))};const r=await importFiles([broken,file(sample())],fresh());assert.equal(r.invalid,1);assert.equal(r.imported,1);});
test('Wochenansicht darf nicht über den gewählten Endzeitpunkt hinausschauen',()=>{const s=sample(),cut=s.events.find(e=>e.type==='quest_completed').timestamp,ds={student:{studentId:s.studentId,catalog:s.catalog},events:s.events,snapshots:[s]};const p=project({studentId:s.studentId,catalog:s.catalog,events:s.events,snapshots:[s],asOf:cut}),h=classHistory([ds],{to:cut,bucket:'week'});assert.equal(h.at(-1).progress,metrics(p).progress);});
test('Zeitstempel müssen echte, kanonische UTC-Datumswerte sein',()=>{const s=sample();s.events[0].timestamp='2026-02-30T08:00:00.000Z';assert.throws(()=>validateSave(s));s.events[0].timestamp='2026-09-01T08:00:00Z';assert.throws(()=>validateSave(s));});
test('Aufgabenereignis ohne taskRunId wird abgelehnt',()=>{const s=sample();delete s.events.find(e=>e.type==='task_started').taskRunId;assert.throws(()=>validateSave(s));});
test('Produktive Worker-Einstiegspunkte verarbeiten Import- und Analyse-Nachrichten',async()=>{const oldSelf=globalThis.self,oldPost=globalThis.postMessage,messages=[];globalThis.self=globalThis;globalThis.postMessage=m=>messages.push(m);try{const name='worker:'+uid();await import('../teacher-dashboard/js/import-worker.js');await self.onmessage({data:{files:[file(sample())],databaseName:name}});assert.equal(messages.at(-1).type,'done');assert.equal(messages.at(-1).result.imported,1);await import('../teacher-dashboard/js/analysis-worker.js');await self.onmessage({data:{id:7,databaseName:name,options:{kind:'summary'}}});assert.equal(messages.at(-1).id,7);assert.equal(messages.at(-1).result.rows.length,1);}finally{globalThis.self=oldSelf;globalThis.postMessage=oldPost;}});

test('Real old 5.1 export, then current curriculum credit: historical denominator remains 37, current 16',async()=>{
 const fs=await import('node:fs');const old=JSON.parse(fs.readFileSync(new URL('./fixtures/legacy-5.1.json',import.meta.url),'utf8'));
 const db=fresh();await db.merge(old,'genuine51.json');const before=await db.detail(old.studentId);assert.equal(metrics(before).totalQuests,37);
 const next=structuredClone(old);next.schemaVersion=3;next.appVersion='6.0.0';next.catalog=CATALOG;next.currentState=validateState(old.currentState);next.exportId=uid();
 const at=new Date(Date.parse(old.exportTimestamp)+1000).toISOString();next.exportTimestamp=new Date(Date.parse(at)+1000).toISOString();
 const s={sessionId:uid(),studentId:old.studentId,startedAt:at,updatedAt:at,finishedAt:null,duration:0,finishReason:null};next.sessions.push(s);
 next.events.push({eventId:uid(),studentId:old.studentId,sessionId:s.sessionId,timestamp:at,type:'curriculum_changed',data:{version:6,previousVersion:'5.1',credited:Object.keys(next.currentState.completed),started:Object.keys(next.currentState.progress)}});
 await db.merge(next,'updated6.json');const current=await db.detail(old.studentId);assert.equal(metrics(current).totalQuests,16);assert.equal(metrics(current).attemptedRuns,0,'old easy attempts must not count as new evidence');
 assert.equal(metrics(await db.detail(old.studentId,old.exportTimestamp)).totalQuests,37);
 assert.equal(metrics(await db.detail(old.studentId,at)).totalQuests,16,'curriculum change before next export uses new catalog');
 const amount=(await db.all('events')).length;await db.merge(old,'old-again.json');await db.merge(next,'new-again.json');assert.equal((await db.all('events')).length,amount);
 const r=await report(db);assert.equal(r.skills.length,0,'old skill data not silently attached to current tasks');
 const historical=await report(db,{range:{to:old.exportTimestamp}});assert.ok(historical.skills.length>0,'old skill data remains visible with its own cutoff');
});
