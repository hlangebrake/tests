{
  "environment": "Chromium under Xvfb; bundled source and in-memory storage test adapter, no real persistence",
  "errors": [],
  "checks": [
    "Actual export button produces JSON with safe student filename",
    "Resume retains identity and taskRunId, second answer is attempt 2; unit finish closes session",
    "UI multi-import older + newer + repeated file yields one student and no duplicate events",
    "Dashboard reload via serialized TEST storage keeps the same attempt count",
    "Student file-input import on another profile adopts the original ID and complete event history",
    "Export with missing name waits for profile input, then resumes with unchanged studentId"
  ]
}